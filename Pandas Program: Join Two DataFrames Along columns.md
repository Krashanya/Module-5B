# 🧪 Pandas Program: Join Two DataFrames Along columns

## 🎯 AIM

To write a Python program using Pandas to **join two DataFrames along column**

---

## 🧠 ALGORITHM

1. **Import Libraries**: Import the `pandas` library.
2. **Create First DataFrame**: Use a dictionary to create `d1`.
3. **Create Second DataFrame**: Use another dictionary to create `d2`.
4. **Merge DataFrames**: Use `pd.merge()` with `axis=0` to merge both DataFrames along column.
5. **Display Result**: Print the new combined DataFrame.

---

## 💻 Program

<img width="455" height="238" alt="image" src="https://github.com/user-attachments/assets/973d7f84-aced-44e9-a3e5-5263c21446a4" />

## Output

<img width="1071" height="512" alt="image" src="https://github.com/user-attachments/assets/75f1ea37-2b3e-4f15-9844-0046d7ba8200" />

## Result
Thus a Python program using Pandas to **join two DataFrames along column**
